td
  = object.name
td.actions
  = smart_listing_item_actions [ { name: :edit, url: edit_mode_path(object) },
    { name: :destroy, url: mode_path(object),
      confirmation: t('smart_listing.msgs.destroy_confirmation') }]
